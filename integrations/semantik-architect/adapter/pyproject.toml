[build-system]
requires = ["hatchling>=1.25,<2"]
build-backend = "hatchling.build"

[project]
name = "koa-semantik-architect-adapter"
version = "0.1.0"
description = "kOA boundary adapter for the separately owned SemantiK Architect subsystem"
readme = "../README.md"
requires-python = ">=3.11"
dependencies = [
  "koa-interfaces>=1.0,<2.0",
]

[project.entry-points."koa.integrations"]
semantik_architect = "koa_semantik_architect_adapter.bootstrap:create_adapter"

[dependency-groups]
dev = [
  "pytest>=8.2,<10",
]

[tool.hatch.build.targets.wheel]
packages = ["src/koa_semantik_architect_adapter"]

[tool.pytest.ini_options]
addopts = "-ra --strict-markers --strict-config"
pythonpath = ["src", "../../../interfaces/python/src"]
testpaths = ["../tests"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_configs = true
